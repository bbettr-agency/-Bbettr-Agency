# Session Handover — 2026-06-14

Branch: `claude/quickbooks-reintegration`

Today's work covered two themes: finishing the **QuickBooks invoicing** polish
(service packages, portal invoice numbers, optional monthly retainer) and
building **PayFast** payments for international clients end-to-end (V1 link
generation → rep-side copy link → live/sandbox fix → V2 ITN webhook).

---

## 1. Completed today

### QuickBooks polish
- **Invoice success handling** — treat `Invoice.Id` as the success signal; no
  longer fail an otherwise-created invoice when `DocNumber` is briefly missing.
- **Structured service packages** — invoices now map to the correct QuickBooks
  product/service and description (migration `0013`).
- **Portal-assigned invoice numbers** — sequential `BBTTR-000001` style numbers
  generated by the portal and shown on the QuickBooks PDF/email (migration
  `0014`).
- **Optional monthly retainer** — a deal can carry a second retainer line that
  is added to the QuickBooks invoice (migration `0015`).

### PayFast (international clients only — South African clients keep EFT)
- **V1 — payment links** (`0016`): on approval of an **international** deal,
  after the QuickBooks invoice is created, the portal generates a signed PayFast
  payment link and stores it. Admin Invoice Requests shows a PayFast badge +
  **Copy link** + **Mark paid**. Public checkout hand-off at `/pay/<id>` plus
  `/pay/return` and `/pay/cancel` pages. Manual "Mark as paid" for confirmation.
- **Rep-side visibility** — Rep Portal → My Deals now has a **Payment** column:
  `EFT` for SA; `Pending` + **Copy payment link** / `Paid` for international;
  "Payment link unavailable — contact admin" if generation failed. Read-only,
  scoped by RLS to the owning rep (+ admins).
- **Live/sandbox switch fix** — `PAYFAST_ENVIRONMENT` now accepts
  `live` / `production` / `prod` (previously only the literal `live`, so
  `production` silently fell back to sandbox). Added an admin **Integrations →
  PayFast** diagnostics card (environment, resolved process URL, merchant id,
  app url, passphrase set, ITN enabled) — **no secrets shown**.
- **V2 — ITN webhook** (`0017`): `POST /api/payfast/notify` auto-marks a payment
  `paid` on a verified `COMPLETE` notification. Verifies signature + does a
  server-to-server validate postback + checks merchant id + checks amount.
  Idempotent, tamper-resistant, always returns 200, logs no secrets. Manual
  "Mark as paid" retained as fallback. Kill-switch via `PAYFAST_ITN_ENABLED`.

---

## 2. Migrations

Run in order in the Supabase SQL Editor (each is idempotent / additive).

| # | File | What it does |
|---|------|--------------|
| 0013 | `0013_deal_service_package.sql` | Structured service-package fields on deals (correct QBO product/service + description). |
| 0014 | `0014_qbo_invoice_docnumber.sql` | Portal-assigned sequential invoice numbers (`BBTTR-000001`) incl. `next_qbo_invoice_docnumber()`. |
| 0015 | `0015_deal_monthly_retainer.sql` | Optional monthly retainer fields on deals (second QBO invoice line). |
| 0016 | `0016_payfast_payments.sql` | `payfast_payments` table (1:1 with invoice request, UNIQUE → no duplicate links), status check, admin RLS + rep read-own; service-role writes. Includes `raw_itn`, `pf_payment_id`, `paid_at` for V2. |
| 0017 | `0017_payfast_itn.sql` | Additive **nullable** ITN audit columns: `pf_payment_status`, `amount_gross`, `itn_received_at`, `itn_valid`, `itn_error`. |

**Status:** 0013–0016 reported applied and tested in production earlier today;
**0017 must be run** alongside the V2 deploy. Confirm `0017` has been executed
before relying on the ITN webhook.

---

## 3. Environment variables (Vercel)

### QuickBooks (existing)
| Var | Notes |
|-----|-------|
| `QBO_CLIENT_ID` | QuickBooks app client id |
| `QBO_CLIENT_SECRET` | QuickBooks app client secret |
| `QBO_REDIRECT_URI` | OAuth redirect (must match the QBO app config) |
| `QBO_ENVIRONMENT` | `production` for the live company, else sandbox |
| `QBO_TOKEN_SECRET` | AES-256-GCM key for token encryption at rest (`openssl rand -base64 32`) |

### PayFast
| Var | Notes |
|-----|-------|
| `PAYFAST_MERCHANT_ID` | Live merchant id |
| `PAYFAST_MERCHANT_KEY` | Live merchant key (**secret** — never displayed) |
| `PAYFAST_PASSPHRASE` | Dashboard passphrase; **required** (signature consistency) |
| `PAYFAST_ENVIRONMENT` | `live` or `production` (or `prod`) → live; anything else → sandbox |
| `PAYFAST_ITN_ENABLED` | Optional; default `true`. Set `false` to disable webhook auto-marking (manual only) |

### Shared
| Var | Notes |
|-----|-------|
| `NEXT_PUBLIC_APP_URL` | Public origin (no trailing slash). Used to build PayFast link, `return_url`, `cancel_url`, and the `notify_url`. Must be correct or ITNs won't arrive. |
| `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, service-role key | Existing Supabase config (unchanged). |

If any PayFast var is missing, PayFast reports "not configured" and international
deals simply get no link — QuickBooks + EFT continue unaffected.

---

## 4. QuickBooks status

- **Live and in use.** Approving an invoice request creates/raises the invoice
  in the connected QuickBooks company (home currency ZAR) and QuickBooks emails
  it to the client. The portal-assigned number (`BBTTR-…`) appears on the PDF.
- Optional monthly retainer adds a second line when present on the deal.
- Connection status + environment are visible on **Admin → Integrations**.
- **Unchanged today by the PayFast work** — PayFast is fully decoupled; a PayFast
  failure never affects invoicing, and invoicing never depends on PayFast.

---

## 5. PayFast status

- **V1 live and tested with a real payment.** International deals get a payment
  link after invoicing; admin and rep can copy it; admin can Mark Paid.
- **Live/sandbox** controlled by `PAYFAST_ENVIRONMENT` (now accepts
  `production`). Verify on **Admin → Integrations → PayFast**: badge = Live,
  process URL = `https://www.payfast.co.za/eng/process`.
- **V2 ITN webhook** deployed in code (`/api/payfast/notify`). Once `0017` is run
  and the deploy is live, a verified `COMPLETE` notification flips a payment to
  `paid` automatically. Manual Mark Paid remains as fallback.
- **South African clients are unaffected** — they pay by EFT and never receive a
  PayFast link.

### ITN verification (all must pass before a "paid" write)
1. MD5 signature recomputed over posted fields (posted order) + passphrase.
2. Server-to-server validate postback to PayFast returns `VALID`.
3. `merchant_id` matches.
4. `amount_gross` matches the stored amount (R0.01 tolerance).
5. `payment_status` drives the transition (`COMPLETE` → paid).

---

## 6. Known issues / watch-items

- **0017 must be applied** before the ITN webhook can write its audit columns;
  if missing, ITN writes will error (and be retried) — manual Mark Paid still
  works in the meantime.
- **ITN needs a public URL + outbound egress.** PayFast can't reach localhost;
  test on a deployed environment. The validate postback needs outbound HTTPS to
  `payfast.co.za` — on Vercel this is fine. If egress is ever blocked, the ITN
  fails closed (rejected + retried; manual fallback available).
- **`NEXT_PUBLIC_APP_URL` correctness is critical** — the `notify_url`,
  `return_url`, and `cancel_url` are derived from it. A wrong value means ITNs
  never arrive (payments would then need manual Mark Paid).
- **PayFast is ZAR-only.** International clients pay the ZAR invoice amount by
  card; there is no multi-currency conversion.
- **No automated tests** were added for the PayFast/ITN flow; verification today
  was manual (real payment for V1; V2 verified by build + design review).
- **Admin debug card** on Integrations is intentionally non-secret and left in
  place; can be removed or gated later if desired.
- **Clipboard copy** on the rep "Copy payment link" button requires a secure
  context (HTTPS) — silently no-ops on plain-HTTP previews (works on the live
  domain).

---

## 7. Next recommended tasks

1. **Run migration 0017** and confirm the V2 deploy, then do an end-to-end ITN
   test (real/sandbox payment → record auto-flips to `paid`; replay = no-op;
   tampered amount / wrong merchant / bad signature = rejected with `itn_error`).
2. **Optional: "payment received" notification** to the rep on a verified
   `COMPLETE` ITN (deliberately deferred in V2).
3. **Optional: reconcile/expire stale links** — surface payments stuck `pending`
   for N days for admin follow-up.
4. **Optional: emailing the PayFast link** to the international client directly
   (today it is copy/paste only — auto-send was intentionally not built).
5. **Consider automated tests** for the signature builder and ITN verification
   (pure functions — easy to unit test).
6. **Tidy the temporary admin debug card** (remove or gate behind a flag) once
   live operation is confirmed stable.

---

## 8. Rollback instructions

### PayFast ITN (V2)
- **Fastest:** set `PAYFAST_ITN_ENABLED=false` in Vercel → the webhook
  acknowledges (200) and mutates nothing; reverts to manual Mark Paid only. **No
  redeploy needed.**
- **Full removal:** `git revert` the V2 commit
  (`299433c` — PayFast ITN webhook). Migration `0017` is additive/nullable and
  safe to leave in place; no destructive down-migration is required.

### PayFast V1 (links)
- Disable by removing the `PAYFAST_*` env vars → international deals stop getting
  links; QuickBooks + EFT continue. To fully remove, revert commits
  `5bd992b` (V1), `abd8a58` (rep copy link), `08f6294` (live-switch fix).
- `payfast_payments` (migration `0016`) is additive; leaving the table in place
  is harmless.

### QuickBooks polish
- Each item is an isolated commit and can be reverted independently:
  `9b92e13` (retainer), `3ec9e02` (invoice numbers), `db4f7eb` (service
  packages), `42f4854` (DocNumber success handling). Their migrations
  (`0013`–`0015`) are additive.

### General
- Manual **Admin → Invoice Requests → Mark Paid** is always available as the
  payment-confirmation fallback regardless of ITN/webhook state.
- All commits today are on `claude/quickbooks-reintegration`; nothing was
  force-pushed or rebased.
